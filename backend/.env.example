# No UC credentials or cookies belong in here anymore -- login happens
# live per-user via CAS (see backend/casClient.js), nothing is persisted.

# comma separated origins allowed to call this backend
CORS_ORIGIN=http://localhost:5173

PORT=8787
